{
  "name": "dan-task-manager-frontend",
  "private": true,
  "scripts": {
    "test": "npm run test:unit",
    "test:unit": "tsc -p tsconfig.spec.json && node --test out-tsc/spec/app/tasks/*.spec.js"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
